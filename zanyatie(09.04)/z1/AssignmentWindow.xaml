<Window x:Class="z1.AssignmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Задания"
        Height="500"
        Width="700"
        WindowStartupLocation="CenterOwner"
        Background="#F5F7FA">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#555"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="AssignmentTemplate">
            <Border Background="White"
                    Padding="12"
                    Margin="0,5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="{Binding Title}"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="#2C3E50"/>

                        <TextBlock Text="{Binding Description}"
                                   FontSize="12"
                                   Foreground="#7F8C8D"
                                   Margin="0,5,0,0"
                                   TextWrapping="Wrap"/>

                        <TextBlock Margin="0,5,0,0"
                                   FontSize="11"
                                   Foreground="#95A5A6">
                            <Run Text="Срок: "/>
                            <Run Text="{Binding DueDate, StringFormat='dd.MM.yyyy'}"/>
                        </TextBlock>
                    </StackPanel>

                    <Button Grid.Column="1"
                            Content="..."
                            Style="{StaticResource IconButton}"
                            Visibility="{Binding DataContext.IsTeacher, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVisibility}}"
                            Click="MenuButton_Click"
                            Tag="{Binding}"/>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Задания"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Foreground="#2C3E50"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,15"/>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ComboBox x:Name="SubjectCombo"
                      Grid.Column="0"
                      Height="38"
                      FontSize="13"
                      Padding="10,0"
                      BorderBrush="#BDC3C7"
                      BorderThickness="1"
                      VerticalContentAlignment="Center"
                      SelectedIndex="0">
                <ComboBoxItem Content="Выберите предмет"/>
                <ComboBoxItem Content="Математика"/>
                <ComboBoxItem Content="Физика"/>
                <ComboBoxItem Content="Программирование"/>
                <ComboBoxItem Content="Английский язык"/>
                <ComboBoxItem Content="Русский язык"/>
                <ComboBoxItem Content="Литература"/>
                <ComboBoxItem Content="Химия"/>
                <ComboBoxItem Content="Биология"/>
                <ComboBoxItem Content="История"/>
            </ComboBox>

            <Button x:Name="AddButton"
                    Grid.Column="1"
                    Content="Добавить"
                    Style="{StaticResource ModernButton}"
                    Background="#27AE60"
                    Height="38"
                    Width="100"
                    Margin="10,0,0,0"
                    Click="AddButton_Click"
                    Visibility="{Binding IsTeacher, Converter={StaticResource BoolToVisibility}}"/>
        </Grid>

        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto">

            <StackPanel>
                <TextBlock x:Name="EmptyTextBlock"
                           Text="Выберите предмет"
                           FontSize="14"
                           Foreground="#95A5A6"
                           HorizontalAlignment="Center"
                           Margin="0,50,0,0"
                           Visibility="Visible"/>

                <ItemsControl x:Name="AssignmentsItemsControl"
                              ItemTemplate="{StaticResource AssignmentTemplate}"/>
            </StackPanel>
        </ScrollViewer>

        <Border Background="White"
                Padding="20,15"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                x:Name="LoadingBorder"
                Visibility="Collapsed">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Загрузка..." Foreground="#2C3E50" FontSize="14"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>